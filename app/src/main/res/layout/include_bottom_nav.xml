<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/bottomNavigation"
    android:layout_width="match_parent"
    android:layout_height="90dp"
    android:background="@android:color/transparent"
    android:clipChildren="false"
    android:clipToPadding="false">




    <!-- Bottom Bar Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="68dp"
        android:layout_gravity="bottom"
        android:background="@drawable/bg_bottom_nav_bar"
        android:gravity="center"
        android:orientation="horizontal"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:paddingStart="4dp"
        android:paddingEnd="4dp">

        <LinearLayout
            android:id="@+id/nav_inventory"
            style="@style/SmartExpBottomNavItem">

            <ImageView
                style="@style/SmartExpBottomNavIcon"
                android:src="@drawable/ic_nav_inventory"
                app:tint="@color/bottom_nav_item" />

            <TextView
                style="@style/SmartExpBottomNavLabel"
                android:text="Inventory" />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/nav_stats"
            style="@style/SmartExpBottomNavItem">

            <ImageView
                style="@style/SmartExpBottomNavIcon"
                android:src="@drawable/ic_nav_stats"
                app:tint="@color/bottom_nav_item" />

            <TextView
                style="@style/SmartExpBottomNavLabel"
                android:text="Stats" />
        </LinearLayout>

        <!-- Placeholder Spacer for Center FAB -->
        <View
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1.2"
            android:visibility="invisible" />

        <LinearLayout
            android:id="@+id/nav_recipes"
            style="@style/SmartExpBottomNavItem">

            <ImageView
                style="@style/SmartExpBottomNavIcon"
                android:src="@drawable/ic_nav_agent"
                app:tint="@color/bottom_nav_item" />

            <TextView
                style="@style/SmartExpBottomNavLabel"
                android:text="Agent" />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/nav_settings"
            style="@style/SmartExpBottomNavItem">

            <ImageView
                style="@style/SmartExpBottomNavIcon"
                android:src="@drawable/ic_nav_settings_gear"
                app:tint="@color/bottom_nav_item" />

            <TextView
                style="@style/SmartExpBottomNavLabel"
                android:text="Settings" />
        </LinearLayout>
    </LinearLayout>

    <!-- Glow Shadow for Center Add Button (aligned to bottom) -->
    <View
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="-2dp"
        android:background="@drawable/bg_orange_glow_shadow"
        android:alpha="0.95" />

    <!-- Center Floating Action Button (Add Product, aligned to bottom) -->
    <FrameLayout
        android:id="@+id/nav_add"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="18dp"
        android:background="@drawable/bg_bottom_nav_add"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackgroundBorderless"
        android:elevation="6dp">

        <ImageView
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_plus"
            app:tint="@color/bottom_nav_add_icon" />
    </FrameLayout>
</FrameLayout>
